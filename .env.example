# Server-only secret for signed training sessions.
# Generate with: node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
TRAINING_SESSION_SECRET=

# Stripe: server-side only. Never use a VITE_ prefix or commit real values.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=
RESEND_API_KEY=
